{
  "name": "plant-backend",
  "version": "1.0.0",
  "description": "Backend for the Plant Guard application",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "esbuild server.js --bundle --platform=node --target=node18 --outfile=dist/server.js",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "express",
    "plant-identification",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "plant-backend": "file:"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
